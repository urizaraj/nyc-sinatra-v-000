%h1 Figure #{ @figure.name }
%p
  %a(href="/figures/#{@figure.id}/edit") Edit this figure
%h3= @titles
%h4= 'This figure had no titles' if @titles == ""

%h2 Landmarks
- @landmarks.each do |landmark|
  %h3= landmark.name

%h4= 'This figure had no landmarks' if @landmarks.size.zero?
